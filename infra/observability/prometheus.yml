global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agentshield-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - ${AGENTSHIELD_API_TARGET:-api:8000}
