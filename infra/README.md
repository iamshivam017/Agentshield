# Infrastructure

Deployment and infrastructure definitions will be added after the local application stack is stable. Infrastructure must reflect the actual deployment target rather than speculative cloud resources.
